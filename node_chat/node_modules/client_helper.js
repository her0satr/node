module.exports = {
	get_array: function(param) {
		var array_user = [];
		for (var i = 0; i < param.length; i++) {
			array_user.push({ user_name: param[i].user_name });
		}
		
		return array_user;
	}
}
